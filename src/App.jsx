//import statements
import { OneEpisode } from './data'
import { useState } from 'react'
import { episodeList } from './data'


export default function App() {
  const [episode] = useState (OneEpisode);
  const [selectedEpisode, setSelectedEpisode] = useState(nul);
//OneEpisode ((id, title, description)) {
  
return (
   <div className="App">
    {OneEpisode.map(OneEpisode id=><{episode.id} title={OneEpisode.title} description={One.Episode.description}/>)}
   </div>

export default function App() {
  
  // variables and javascript logic

  //return(JSX and Javascript)


}
